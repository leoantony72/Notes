

### Windows Installation
------
1. Install the latest version of the git from the web.
2. Do the recommended actions in the git installer, don't change the path to installation
3. Check for version, git --version
4. Run the following commands

```git
git config --global user.name "YOUR NAME"
git config --global user.email "@gmail.com"
```

